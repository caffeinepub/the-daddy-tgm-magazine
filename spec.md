# Specification

## Summary
**Goal:** Remove the publish detail UI element (deployment/attribution metadata) from the app so it is no longer visible to users.

**Planned changes:**
- Remove the publish detail / caffeine.ai attribution element from the app's layout globally across all pages

**User-visible outcome:** The publish detail element no longer appears anywhere in the app; the rest of the layout remains unchanged.
